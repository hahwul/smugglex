---
name: Homebrew tap Publish
on:
  release:
    types: [published]
jobs:
  homebrew-releaser:
    runs-on: ubuntu-latest
    name: homebrew-releaser
    steps:
      - name: Release smugglex to Homebrew tap
        uses: Justintime50/homebrew-releaser@v3
        with:
          homebrew_owner: hahwul
          homebrew_tap: homebrew-smugglex
          formula_folder: Formula
          github_token: ${{ secrets.SMUGGLEX_PUBLISH_TOKEN }}
          commit_owner: hahwul
          commit_email: hahwul@gmail.com
          depends_on: |
            "rust"
          install: |
            system "cargo build --release"
            bin.install "target/release/smugglex"
          test: system "{bin}/smugglex", "-V"
          update_readme_table: false
          skip_commit: false
          debug: false
